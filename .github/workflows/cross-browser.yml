name: Test

on: [ push ]
jobs:
  tests:
    name: Run Tests

    permissions:
      contents: write
      pages: write
      id-token: write

    strategy:
      fail-fast: false
      matrix:
        platform:
          - windows
          - mac
          - linux
        app:
          - chrome
          - firefox
        include:
          - platform: mac
            app: safari
          - platform: windows
            app: edge
          - platform: mac
            app: edge
          - platform: ios
            app: safari
          - platform: android
            app: chrome

    uses: ./.github/workflows/run-ui-test.yml
    with:
      platform: ${{ matrix.platform }}
      app: ${{ matrix.browser }}
    secrets: inherit

